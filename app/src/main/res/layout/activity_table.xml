<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#515151">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewTables"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/fabAddTable"
        android:background="#cfcccc"
        android:padding="16dp" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddTable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:contentDescription="Add Table"
        android:src="@drawable/ic_add_table"
        app:backgroundTint="#cfcccc" />

    <TextView
        android:id="@+id/textViewDatabaseName"
        android:layout_width="291dp"
        android:layout_height="59dp"
        android:layout_alignTop="@+id/fabAddTable"
        android:layout_alignBottom="@+id/fabAddTable"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="13dp"
        android:layout_marginTop="2dp"

        android:layout_marginEnd="107dp"
        android:layout_marginBottom="-5dp"
        android:text="TextView"
        android:textColor="@color/white"
        android:textSize="24dp" />

</RelativeLayout>
